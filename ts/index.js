export function handlePortMessage2(message) {
    // Your complex logic here
    console.log("Received message from Elm port:", message);
  
    // Perform additional processing or actions based on the message
    // (e.g., network requests, DOM manipulation, etc.)
  
    // You can optionally return a value or perform side effects here
  }